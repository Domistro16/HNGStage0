# Server
PORT=3000
NODE_ENV= # development or production if you are ready to push to production

# Cat Facts API (override for tests or mirrors)
CATFACT_URL=https://catfact.ninja/fact
CATFACT_TIMEOUT_MS=5000    # timeout in milliseconds for the cat api request

# Rate limiting
RATE_LIMIT_MAX=60         # max requests per IP per minute

# Fallback / UX
FALLBACK_CAT_FACT=

# User object returned by /me
USER_NAME=
USER_EMAIL=
USER_STACK=

# CORS (optional - set to '*' for dev or a specific origin in production)
CORS_ORIGIN=
