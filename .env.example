# Server
PORT=3000
NODE_ENV=development

# Cat Facts API (override for tests or mirrors)
CATFACT_URL=https://catfact.ninja/fact
CATFACT_TIMEOUT_MS=5000    # timeout in milliseconds for the external request

# Rate limiting
RATE_LIMIT_MAX=60         # max requests per IP per minute

# Fallback / UX
FALLBACK_CAT_FACT="Cat fact currently unavailable. Please try again later."

# User object returned by /me
USER_NAME="Esih Egwurube"
USER_EMAIL=desmondesih@gmail.com
USER_STACK="NodeJS/Express"

# CORS (optional - set to '*' for dev or a specific origin in production)
CORS_ORIGIN=*
